{
  "permissions": {
    "allow": [
      "Bash(sqlite3 promti.db \".tables\")",
      "Bash(sqlite3 promti.db \".schema\")",
      "Bash(sqlite3 promti.db \"SELECT * FROM phrases LIMIT 5;\")",
      "Bash(sqlite3 promti.db \"SELECT * FROM forbidden_words LIMIT 10;\")"
    ]
  }
}
